<%= content_for :modal_title, 'Edit profile' %>
<%= render partial: 'form' %>

<small class="flex justify-end mt-6">
    <i class="text-gray-400">Unhappy?</i>&nbsp;<%= link_to "Cancel my account", registration_path(resource_name), class: "link link--semibold", data: { confirm: "Are you sure?" }, method: :delete %>
</small>
